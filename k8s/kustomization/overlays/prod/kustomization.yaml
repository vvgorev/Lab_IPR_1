apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: telegram-prod
resources:
  - ../../base
patches:
  - target:
      kind: Deployment
      name: telegram-bot
    patch: |-
      - op: replace
        path: /spec/replicas
        value: 3
secretGenerator:
  - name: bot-secrets
    envs:
      - secret.env